-- Code generated by campusctl from a module schema. DO NOT EDIT.
-- Source: schemas/activity.yaml

CREATE TABLE activities (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    title VARCHAR(200) NOT NULL,
    summary VARCHAR(500) NOT NULL,
    body LONGTEXT NOT NULL,
    location VARCHAR(500) NOT NULL,
    signup_start_at DATETIME(3) NOT NULL,
    signup_end_at DATETIME(3) NOT NULL,
    start_at DATETIME(3) NOT NULL,
    end_at DATETIME(3) NOT NULL,
    capacity BIGINT NOT NULL,
    registered_count BIGINT NOT NULL,
    status VARCHAR(24) NOT NULL,
    review_status VARCHAR(24) NOT NULL,
    review_comment VARCHAR(500) NULL,
    contact_type VARCHAR(16) NOT NULL,
    contact_ciphertext VARCHAR(4096) NOT NULL,
    created_by BIGINT UNSIGNED NOT NULL,
    updated_by BIGINT UNSIGNED NOT NULL,
    version BIGINT UNSIGNED NOT NULL,
    created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    KEY idx_activities_signup_start_at (signup_start_at),
    KEY idx_activities_signup_end_at (signup_end_at),
    KEY idx_activities_start_at (start_at),
    KEY idx_activities_end_at (end_at),
    KEY idx_activities_status (status),
    KEY idx_activities_review_status (review_status),
    KEY idx_activities_created_by (created_by),
    KEY idx_activities_updated_by (updated_by),
    KEY idx_activity_public_list (status, review_status, start_at),
    KEY idx_activity_admin_list (status, review_status, start_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;

CREATE TABLE activity_registrations (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    activity_id BIGINT UNSIGNED NOT NULL,
    user_id BIGINT UNSIGNED NOT NULL,
    status VARCHAR(16) NOT NULL,
    registered_at DATETIME(3) NOT NULL,
    cancelled_at DATETIME(3) NULL,
    idempotency_key VARCHAR(128) NOT NULL,
    version BIGINT UNSIGNED NOT NULL,
    created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    KEY idx_activity_registrations_status (status),
    UNIQUE KEY uk_activity_registration_user (activity_id, user_id),
    UNIQUE KEY uk_activity_registration_idempotency (activity_id, user_id, idempotency_key),
    KEY idx_activity_registration_user (user_id, status, registered_at),
    FOREIGN KEY (activity_id) REFERENCES activities(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;

